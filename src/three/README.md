# Volume stage (parked)

This folder is the scroll-driven Three.js volume: moon, LED ring, black-hole disc, orbiting cubes, and per-project artifacts.

The default site is the AI session HUD. This stage is kept so it can come back.

**Restore:** open the site with `?stage=volume`

Do not delete this folder to “clean up” the new look — that is the save.
